<analysis>**original_problem_statement**: 
Build an AI-powered autonomous financial advisor (Agentic Wealth System) for the Indian market. PRODUCT REQUIREMENTS: Implement portfolio parsing (CAS PDF/CSV), Google OAuth, multi-factor risk insights, admin whitelist, actionable insights dashboard, live equity/ETF pricing, scenario simulation, Onboarding flows, Risk Profile questionnaire, Chat Sessions, and Markdown support. Must ensure data security and compliance (DPDP Act).

**User's preferred language**: 
English (INSTRUCT next agent to respond in English only)

**what currently exists?** 
A full-stack financial dashboard featuring Google OAuth, Onboarding Flow, Risk Profile questionnaire, and AI chat sessions. The CAS parsing logic has been completely revamped: instead of using purely local OCR, the app now uses the external  API and its React SDK () to handle PDF uploads and Gmail fetching. Master data (AMFI, NSE BhavCopy) is synced locally for cross-referencing. A data reset script is available to wipe portfolio states.

**Last working item**: 
    - Last item agent was working: Fixing the Gmail import via CAS connect flow which is failing. The agent discovered that the CAS Parser SDK requires an OAuth callback page/route to handle the  logic.
    - Status: IN PROGRESS
    - Agent Testing Done: N
    - Which testing method agent to use?: Frontend testing agent / manual testing (Screenshot tool)
    - User Testing Done: N

**All Pending/In progress Issue list**:  
  Issue 1: Gmail import via CAS connect SDK is failing (P0)
     - Attempted fixes: Investigated SDK documentation.
     - Next debug checklist: Create a dedicated OAuth callback route in the React frontend (e.g., ) and implement the  logic from the  SDK so it successfully posts the token back to the widget.
     - Why fix this issue and what will be achieved with the fix?: Required for end-to-end functionality of the user-requested SDK widget.
     - Status: IN PROGRESS
     - Is recurring issue? N
     - Should Test frontend/backend/both after fix?: Frontend
     - Blocked on other issue: None
     
  Issue 2: Legacy Google OAuth Login Broken () (P1)
     - Attempted fixes: Investigated in previous forks. 
     - Next debug checklist: Wait for user to add the generated preview URL to their Google Cloud Console Authorized JavaScript origins.
     - Why fix this issue: Absolute blocker for non-whitelisted standard logins.
     - Status: USER VERIFICATION PENDING
     - Is recurring issue? Y

**In progress Task List**: 
  Task 1: Complete SDK Integration for CAS Parser
     - Where to resume: Implement the  callback page for the SDK's Gmail flow.
     - What will be achieved with this?: Users can seamlessly pull CAS files directly from their Gmail using the widget.
     - Status: IN PROGRESS
     - Should Test frontend/backend/both after fix?: Frontend
     - Blocked on something: Nothing.

**Upcoming and Future Tasks**
  - P0: Security Upgrades (DPDP Act Compliance) — Implement PAN field encryption (AES-256), robust Consent logging, and Audit trails.
  - P0: Retire legacy upload path and old Gmail Import tab — Since the new SDK widget handles everything reliably, the old custom UI components should be removed to avoid confusing users.
  - P0:  refactoring — File is critically bloated (>2800 lines). Break down routes into .
  - P1: Goal-based planning module (Retirement, Child Education) with AI-calculated SIPs.
  - P2: Portfolio versioning (Delta tracking).
  - P2: PostgreSQL migration for relational structure.

**Completed work in this session**
- Swapped masterdata sources to use user-provided CSVs (AMFI, NSE BhavCopy, Equity List, SGB). (DONE)
- Attempted local PaddleOCR & Tesseract+img2table refactoring, but eventually discarded it due to user pivot. (DONE)
- Integrated  API () with  compression to handle the 1.8MB API limit for scanned PDFs. (DONE)
- Added  React SDK to the frontend and wired up . (DONE)
- Created  and wiped all existing users' holdings, insights, and onboarding flags to test the flow fresh. (DONE)
- Fixed SDK named export bug ( vs ). (DONE)

**Earlier issues found/mentioned but not fixed**
  None specific.

**Known issue recurrence from previous fork**
  - Issue recurrence in previous fork: System packages (e.g. poppler-utils, tesseract) sometimes get uninstalled on container restart. 
  - Recurrence count: 4
  - Status: RESOLVED via startup scripts, but remain vigilant if PDF libraries fail.

**Code Architecture**


**Key Technical Concepts**
- Frontend: React, Tailwind CSS, Shadcn UI,  SDK.
- Backend: FastAPI, MongoDB (Motor), ,  (for PDF compression).

**key DB schema**
  - : 
  - : Contains parsed CAS metadata.
  - : Individual equity/MF holding data.

**changes in tech stack**
  - **MAJOR PIVOT**: The user opted to stop building local OCR logic (Tesseract/PaddleOCR) and instructed the use of the 3rd-party  API to handle all CAS parsing.
  - Installed  and  to dynamically compress large PDFs down to the API's upload limit.

**All files of reference** 
  - 
  - 
  - 
  - 
  - 

**Areas that need refactoring**
  -  is critically bloated. Route definitions must be decoupled.
  - Legacy custom portfolio upload components/tabs in  and  should be removed in favor of the new single SDK widget.

**key api endpoints**
  -  (Mints SDK tokens)
  -  (Handles SDK parsing results)

**Critical Info for New Agent** 
  - The  SDK widget Gmail fetch is currently broken because the SDK expects an OAuth redirect URI configured on the frontend that implements its callback. You must read the  docs to build the callback route (e.g., import  from the SDK and run it on page load).
  - The script  can be used to reset all user onboarding flags and delete holdings if you need a fresh start.
  - API keys for CAS Parser are already configured in the  file.

**documents and test reports created in this job** 
  - 
  -  (Updated)

**Last 10 User Messages and any pending HUMAN messages** 
 - User: P0 Log in, confirm onboarding flow starts fresh, click Launch CAS Connect end-to-end with a real CAS.
 - Agent: Fixed SDK modal display bug (xdg-open - opens a file or URL in the user's preferred application

Synopsis

xdg-open { file | URL }

xdg-open { --help | --manual | --version }

Use 'man xdg-open' or 'xdg-open --manual' for additional info. named export).
 - User: Uploaded Excel artifacts & screenshot. GMAIL import via CAS connect is failing ...what needs to be done here
 - Agent: Began investigating the SDK's  utility and crashed due to context length.
 - (No Pending Human Message, proceed with resolving the SDK Gmail callback).

**Project Health Check:**
Broken (Gmail CAS Connect SDK flow fails due to missing callback page setup).

**3rd Party Integrations** 
 - CAS Parser API (casparser.in) — requires **User API Key** (Implemented, keys in ).
 - OpenAI GPT-4o — requires **User API Key**.
 - Google OAuth / Gmail API — requires **User API Key** (Blocked by GCP).

**Testing status**
  - Testing agent used after significant changes: NO 
  - Troubleshoot agent used after agent stuck in loop: NO
  - Test files created: 
  - Known regressions: Legacy upload flow is partially redundant.

**Credentials to test flow:**
Check .

**What agent forgot to execute**
The agent was in the middle of executing the fix for the Gmail CAS Connect SDK flow but ran out of context before writing the frontend callback page. 

**Debugging checklist for current issue**
    Issue: GMAIL import via CAS connect SDK is failing because of missing frontend OAuth callback.

    Frontend files to check: 
    - You must create a new React route/page (e.g., ).
    - In this component, import  from  (or wherever it resides according to the SDK's definitions).
    - Ensure this route is provided as the  configuration to the  method in .
   
    </analysis>
