# Functionality Verification Report — WORK-0140 / 0144 / 0145

- **Branch:** feat/copilot-backtest → origin/dev
- **Date:** 2026-07-07
- **Author:** Claude (Full-Stack Developer + QA Engineer)
- **Environment:** unit (local) — fully verifiable (no DB / fastapi / staging needed).
- **Changed areas:** `backend/nidp/shared/sources/content_guard.py`, `backend/nidp/shared/ingester_base.py`, tests; `backend/nidp/deploy/vm/nidp.cron` (doc).

## Summary
- **WORK-0140 — content-type / magic-byte check:** `sniff_content_type()` detects the actual
  type from magic bytes (zip/gzip/pdf/xls/json/html/xml); `BaseIngester.run()` records it in
  `run.metadata["sniffed_content_type"]` and WARNs + flags `content_type_conflict` when a
  text-expected URL (`.csv`/`.json`/`.xml`) returns a binary/HTML body — closing the "type
  guessed from URL, never checked against bytes" gap (D6). Does NOT fail the run (some feeds
  legitimately serve compressed payloads) — it makes format drift visible.
- **WORK-0144 — feed-list drift guardrail:** `test_feed_registry_drift.py` fails CI when the
  cron schedule, `feed_health_check.EXPECTED_FEEDS`, and `backfill.SPECS` diverge — turning the
  silent list-drift that caused monitoring blind spots (Theme E) into a loud test failure.
- **WORK-0145 — one scheduler:** `nidp.cron` header now declares VM cron the **authoritative**
  scheduler and marks the GCP Cloud Scheduler path DEPRECATED (documents the decommission steps).
  Full deletion of the `nidp-cron-*` triggers is an ops task (needs GCP compute perms).

## Test Cases
| ID | Area | Scenario | Type | Result |
|----|------|----------|------|--------|
| TC-1 | 0140 sniff | zip/gzip/pdf/xls/json/html/xml detected from magic bytes | unit | **PASS** |
| TC-2 | 0140 sniff | plain CSV / empty → None (no false type) | unit | **PASS** |
| TC-3 | 0140 conflict | text URL + binary/HTML body → conflict=True | unit | **PASS** |
| TC-4 | 0140 conflict | match / None / non-text-URL → conflict=False (no noise) | unit | **PASS** |
| TC-5 | 0144 drift | every ERROR-monitored feed is scheduled in cron | unit | **PASS** |
| TC-6 | 0144 drift | every recoverable daily feed is scheduled in cron | unit | **PASS** |
| TC-7 | 0144 drift | the 4 new recovery/monitor jobs are scheduled | unit | **PASS** |

## Test Output (real, unedited)
```
$ python3 -m py_compile content_guard.py ingester_base.py            → compile OK
$ python3 -m pytest nidp/tests/services/test_content_guard.py -q     → 18 passed
$ python3 -m pytest nidp/tests/test_feed_registry_drift.py -q        → 3 passed
$ python3 -m pytest <full feed-reliability suite, non-fastapi> -q    → 116 passed
$ bash nidp/tests/test_run_service_retry.sh                          → ALL BASH RETRY TESTS PASS
```

## Verdict: PASS
