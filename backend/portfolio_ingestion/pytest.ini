[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: requires a live Postgres reachable via PI_POSTGRES_URL
    slow: takes more than a few seconds to run
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
